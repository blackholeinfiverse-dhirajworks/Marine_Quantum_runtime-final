# Marine Quantum Runtime
# ────────────────────────────────────────────────────────────────
# Python >= 3.8 required.
#
# CORE RUNTIME — zero pip installs needed
#   run_signal.py, run_governance.py, run_distributed_qapp.py,
#   run_operational_drift.py, run_quantum_pipeline.py all work
#   with Python stdlib only (math, hashlib, json, dataclasses, etc.)
#
# RECOMMENDED — enables real Aer quantum simulation
qiskit>=1.0.0
qiskit-aer>=0.14.0
#
# REST API SERVER — expose runtime over HTTP
fastapi>=0.100.0
uvicorn[standard]>=0.22.0
pydantic>=2.0.0
#
# HTTP CLIENT MODE — RuntimeClient(base_url=...) in client.py
requests>=2.28.0
#
# API TEST SUITE — tests/test_api_endpoints.py and tests/test_client.py
httpx2>=0.1.0
#
# OPTIONAL — IBM Quantum real hardware execution
# qiskit-ibm-runtime>=0.20.0   # also set IBM_QUANTUM_TOKEN env var
#
# ────────────────────────────────────────────────────────────────
# Install groups:
#   Core only:        (no install needed)
#   + Quantum:        pip install qiskit qiskit-aer
#   + API server:     pip install fastapi uvicorn pydantic
#   + Client SDK:     pip install requests
#   + Testing:        pip install httpx2
#   Everything:       pip install -r requirements.txt
